# 星座命理网页版 · 部署上线指南（GitHub + Render，免费）

> 适用对象：第一次把本项目推上 GitHub 并部署到公网的用户。
> 当前状态：本地代码已 `git commit`（提交 `efcd479`，分支 `main`），远程 `origin` 已指向
> `https://github.com/chengxiaomiao/xingzuo-mingli.git`。只差「推送」和「部署」两步。
> 本指南不含任何 Token / 密码，凭据全程只在你自己的电脑上输入。

---

## 零、开始前确认两件事

1. **GitHub 空仓库已建好**
   - 仓库地址：`https://github.com/chengxiaomiao/xingzuo-mingli.git`
   - 建仓库时 **三样都不要勾**：☐ README ☐ .gitignore ☐ license（保持空仓库，否则 push 会冲突）。
   - 如果你已经建了且带了 README，也没关系，照「常见问题」里的办法处理即可。

2. **已生成 Personal Access Token（PAT）**
   - 路径：GitHub 网页 → 右上角头像 → Settings → 最底部 Developer settings →
     Personal access tokens → Tokens (classic) → Generate new token (classic)
   - Note 随便填（如 `render-deploy`），Expiration 选 7 天，Scope 勾选 **`repo`**（展开后子项全勾）。
   - 生成后复制那串 `ghp_xxxxxxxx` —— **只显示这一次**，先存到备忘录。
   - 推送时它当作「密码」用；推完建议回 GitHub 把它删掉，更安全。

---

## 一、打开终端（macOS）

- 按 `⌘ + 空格` 打开 Spotlight，输入「终端」或「Terminal」，回车。
- 会弹出一个白色/黑色的命令行窗口，光标前面通常有 `%~` 之类的提示符。
- **所有下面的命令都整段复制、粘贴到这个窗口里，然后按回车。**

> 关键提醒：你之前遇到的 `fatal: not a git repository` 是因为当时不在项目目录里。
> 本指南**统一使用「绝对路径 + `git -C`」写法，不依赖你先 `cd`**，从根本上避免这个问题。

---

## 二、第 1 步：验证本地仓库路径（30 秒，防踩坑）

粘贴下面这条命令，确认能找到项目（不会改动任何东西）：

```bash
git -C /Users/ting/WorkBuddy/2026-08-11-20-24-34/xingzuo-mingli-web status
```

- ✅ 正常会显示：`On branch main` 和 `nothing to commit, working tree clean`。
- ❌ 如果报 `not a git repository`：说明这个绝对路径在你电脑上不对（极少见）。
  请打开访达，找到项目文件夹 `xingzuo-mingli-web`，右键 → 显示简介，核对完整路径，
  把上面命令里的路径换成你的实际路径再试。

---

## 三、第 2 步：推送代码到 GitHub

粘贴这条命令（同样用了绝对路径，不用 `cd`）：

```bash
git -C /Users/ting/WorkBuddy/2026-08-11-20-24-34/xingzuo-mingli-web push origin main
```

运行后会要凭据，**按下面填**：

- **Username（用户名）**：`chengxiaomiao`
- **Password（密码）**：**粘贴你的 PAT（`ghp_...` 那串）**，**不是** GitHub 登录密码。

> ⚠️ 终端里粘贴密码时**光标不会动、也不显示任何字符**，这是正常的，粘贴完直接按回车即可。
> （macOS 终端粘贴快捷键：`⌘ + V`）

**成功标志**：出现类似下面的输出，说明推送完成：
```
Enumerating objects: 45, done.
...
   efcd479..xxxxxxx  main -> main
```

---

## 四、第 3 步：在 GitHub 上确认

打开浏览器访问：`https://github.com/chengxiaomiao/xingzuo-mingli`

应该能看到这些文件和文件夹：`server/`、`web/`、`package.json`、`render.yaml`、`README.md`、
`项目交接文档.md` 等。看到就说明代码已经上云了。

---

## 五、第 4 步：部署到 Render（免费，网页点几下）

1. 打开 [render.com](https://render.com) → 点 **Sign Up** / **Log In**。
2. 注册/登录时选 **「Continue with GitHub」**，授权 Render 访问你的 GitHub 账号。
3. 登录后进入 Dashboard，点右上角 **「New」→「Web Service」**。
4. **Connect a repository**：找到 `chengxiaomiao/xingzuo-mingli`，点 **Connect**。
   （如果列表里看不到，点 「Configure」 给 Render 授权读取你的仓库。）
5. 在配置页按下面填：
   - **Name**：`xingzuo-mingli`（任意）
   - **Region**：选 `Singapore`（离国内近）或 `Oregon`
   - **Branch**：`main`
   - **Runtime**：`Node`
   - **Build Command**：`npm install`
   - **Start Command**：`npm start`
   - **Instance Type**：选 **Free**（免费层）
   - **Environment Variables（环境变量）**：**不用填** —— 本项目用本地 SQLite 文件库，
     已不依赖任何外部数据库或密钥。
6. 拉到底点 **「Create Web Service」**。
7. 等待 1~3 分钟，部署日志最后出现 `==> Starting service` / 绿色 `Live` 即成功。
8. 页面顶部会给一个公网地址，形如：
   `https://xingzuo-mingli-xxxx.onrender.com`
   这个地址就是你的线上网站，手机/电脑浏览器都能打开。

---

## 六、第 5 步：访问 & 收尾

- 把上面的 `https://xxxx.onrender.com` 发给我，我会：
  1. 把「项目交接文档.md」里的「上线部署」状态从 ⏳ 改为 ✅，并补上真实地址；
  2. 划掉文档第 5 节的「待上线」清单；
  3. 在文档第 7 节补一句「线上访问地址已生效」。
- 之后你就可以把这个链接发给朋友，用手机打开使用。

---

## 七、重要注意事项（部署前必读）

1. **免费层数据会随重启清空**
   Render 免费实例的磁盘是「临时盘」，每次部署/重启/休眠唤醒后，`data/` 里的 SQLite 数据库
   会被重置 —— 也就是说你录进去的档案、账号会丢失。
   - 对个人演示、临时使用完全够用；
   - 若要**永久保存数据**，需升级到付费实例（带持久盘），或自行挂载外部数据库（更复杂）。
   - 决策点：先免费跑通看效果，需要持久化时再升级。

2. **免费层会休眠、首次访问慢**
   15 分钟无访问后实例会休眠，下次打开要等十几秒「冷启动」。属于正常现象，不是 bug。

3. **HTTPS 默认可用**
   Render 自动给 `onrender.com` 域名配好 HTTPS，不用自己申请证书。

---

## 八、常见问题排查

**Q1：还是 `fatal: not a git repository`**
→ 你粘贴命令时把路径改动了。严格整段复制「第 2 步」的命令，别手敲。

**Q2：`! [rejected]  main -> main (non-fast-forward)` / `failed to push some refs`**
→ 说明 GitHub 上的空仓库其实带了 README，和本地历史冲突。解决（在终端粘贴）：
```bash
git -C /Users/ting/WorkBuddy/2026-08-11-20-24-34/xingzuo-mingli-web pull --rebase origin main
git -C /Users/ting/WorkBuddy/2026-08-11-20-24-34/xingzuo-mingli-web push origin main
```
（若 pull 时要求合并信息，直接 `:wq` 保存退出即可。）

**Q3：`remote: Permission to ... denied` / `403`**
→ PAT 权限不够或填错。确认 PAT 勾了 `repo` 全部子项；密码那里确实填的是 PAT 而非登录密码。

**Q4：终端粘贴密码没反应 / 不显示字符**
→ 正常。Mac 终端输入密码默认不回显，粘贴后直接回车。

**Q5：想把这版《部署上线指南.md》也推到仓库**
→ 它目前还没提交。推送代码成功后，可再加一句提交（在你终端）：
```bash
git -C /Users/ting/WorkBuddy/2026-08-11-20-24-34/xingzuo-mingli-web add 部署上线指南.md
git -C /Users/ting/WorkBuddy/2026-08-11-20-24-34/xingzuo-mingli-web commit -m "docs: 添加部署上线指南"
git -C /Users/ting/WorkBuddy/2026-08-11-20-24-34/xingzuo-mingli-web push origin main
```
（不做这一步也完全不影响上线，指南仅作参考。）

---

## 九、命令速查（复制即用）

```bash
# 1) 验证
git -C /Users/ting/WorkBuddy/2026-08-11-20-24-34/xingzuo-mingli-web status

# 2) 推送（凭据：用户 chengxiaomiao / 密码填 PAT）
git -C /Users/ting/WorkBuddy/2026-08-11-20-24-34/xingzuo-mingli-web push origin main

# 3) 若仓库非空导致冲突
git -C /Users/ting/WorkBuddy/2026-08-11-20-24-34/xingzuo-mingli-web pull --rebase origin main
git -C /Users/ting/WorkBuddy/2026-08-11-20-24-34/xingzuo-mingli-web push origin main
```
