# 星座命理分析 · 手机网页版 — 项目交接文档

> 用途：本文件是项目的完整快照，可在**新的对话**中直接发给我，让我快速恢复上下文并继续开发。
> 最后更新：2026-08-14 ｜ 当前状态：**代码完成、本地端到端自测全过、已上线部署、Turso 持久化已配置（✅ 全部完成）**。线上地址 **https://xingzuo-mingli.onrender.com**。**数据库已从本地 SQLite 迁移到 LibSQL（@libsql/client）+ Turso 云端托管，数据跨设备 / 重部署不丢（详见第 5、7 节）。**

---

## 1. 项目概述与核心技术栈

**产品定位**：一个**手机网页版**的八字命理分析工具，新中式视觉（宣纸米白 + 朱砂 + 黛青 + **再生纸粗糙纹理背景**）。面向多用户（主账号 + 子账号），提供「生辰格局 / 五行分布 / 喜用神」三模块分析与档案管理。定位为「传统干支文化 / 国学知识查询工具」，页面附免责声明。

**路线变更**：早期做过微信小程序（含云开发），但**小程序审核未通过**，已弃用小程序路线，全面转为网页版。小程序代码仍保留在另一个目录 `xingzuo-mingli/`（与本网页版无关，不要混淆）。

**核心技术栈**：
| 层 | 技术 | 说明 |
|----|------|------|
| 前端 | 纯 HTML / CSS / 原生 JS（无框架、无打包） | 8 个页面 + 8 个 JS 文件，直接由 Express 静态托管 |
| 后端 | Node.js + Express 4 | 4 组 API 路由 + 1 个鉴权中间件 |
| 数据库 | LibSQL（@libsql/client，**纯 JS、无需原生编译**） | 配置 `LIBSQL_URL`+`LIBSQL_AUTH_TOKEN` 则连 Turso 等托管库（**数据持久、重部署不丢**）；未配置则回退本地文件 `data/mingli.db` |
| 命理引擎 | lunar-javascript | 四柱/农历/真太阳时计算；喜用神与格局为**规则计算（非 AI）** |
| 部署 | GitHub + Render（免费层） | 已含 `render.yaml`；持久化需在 Render 控制台填 `LIBSQL_URL` / `LIBSQL_AUTH_TOKEN` 两个环境变量 |

**运行环境要求**：Node **22.x**（已在 `package.json` 的 `engines` 锁定——早期 Render 自动选 Node 26 会让 `better-sqlite3`（原方案）编译失败，故固定 22.x；现改用纯 JS 的 `@libsql/client` 后此问题已根治）。`npm install` 无原生编译步骤。

---

## 2. 完整的文件结构与各文件功能说明

```
xingzuo-mingli-web/
├── package.json            # 依赖与启动脚本（start: node server/index.js）；engines.node=22.x
├── package-lock.json       # 锁定依赖版本
├── render.yaml             # Render 部署配置（build npm install / start npm start / 免费层）
├── README.md               # 本地运行 + 上线部署 + 合规说明
├── .gitignore              # 忽略 node_modules/ data/ *.log .DS_Store
├── test-e2e.js             # 端到端自测脚本（自动起服务跑完整流程）
├── 部署上线指南.md          # 给用户的 Step-by-Step 部署图文教程
├── 项目交接文档.md          # 本文件
│
├── server/                 # 后端（Node + Express）
│   ├── index.js            # 入口：connect DB → 挂路由 → 静态托管 web/ → `GET /` 重定向到 /login.html → 监听 PORT(默认3000)
│   ├── db.js               # LibSQL 连接层 + 建表 + getAllowedOwners(跨账号权限集合)；远程(Turso)或本地 file: 自动切换
│   ├── bazi.js             # ★ 命理计算引擎 analyze(birthInfo)（核心，见第4节）
│   ├── middleware/
│   │   └── auth.js         # Bearer token 鉴权中间件（校验登录态 + 账号停用）
│   └── routes/
│       ├── auth.js         # /api/auth → register / login / logout
│       ├── profile.js      # /api/profiles → 档案增删改查（含越权隔离）
│       ├── analyze.js      # /api/analyze → 触发分析并落库 results
│       └── admin.js        # /api/admin → 主账号管理子账号（list / add / disable / enable / remove）
│
└── web/                    # 前端（纯静态，由 Express 托管）
    ├── login.html          # 登录页
    ├── register.html       # 注册页（仅首个账号成为主账号）
    ├── home.html           # 首页/导航（显示当前账号角色）
    ├── entry.html          # 录入/编辑出生信息表单
    ├── profiles.html       # 档案列表（主账号有「全部/仅我的」切换）
    ├── result.html         # ★ 分析结果页（三模块 + 五行 canvas + 免责）
    ├── admin.html          # 主账号管理子账号页
    ├── css/
    │   └── style.css       # 全局新中式样式；再生纸纹理以 base64 SVG 写入 `--paper-mottle`/`--paper-grain` 变量
    └── js/
        ├── common.js       # 通用：token 管理、api() 封装、requireAuth/logout
        ├── login.js        # 登录逻辑
        ├── register.js     # 注册逻辑
        ├── home.js         # 首页角色展示
        ├── entry.js        # 出生信息表单（公历/农历、精确时间/时辰范围、回填编辑）
        ├── profiles.js     # 档案列表渲染 + 分析/编辑/删除 + owner 视角切换
        ├── result.js       # ★ 三模块渲染 + drawWuxing(canvas 五行生克图)
        └── admin.js        # 子账号增删与启停
```

**数据库表（LibSQL，`data/mingli.db` 为本地回退文件，已被 .gitignore 忽略；配置 Turso 后表结构完全相同）**：
- `users`：id, username(唯一), passwordHash, salt, role(owner/member), owner(主账号用户名), status(active/disabled), token, tokenExpire, createdAt
- `profiles`：id, owner, personName, birthInfo(JSON 文本), createdAt, updatedAt
- `results`：id, owner, recordId, personName, result(JSON 文本), createdAt, updatedAt，`UNIQUE(owner, recordId)`

---

## 3. 已实现的所有功能模块列表及其当前状态

| # | 功能模块 | 状态 | 说明 |
|---|----------|------|------|
| 1 | 账号体系（注册/登录/退出） | ✅ 完成 | 首个注册账号自动成主账号；之后公共注册关闭，由主账号添加子账号 |
| 2 | 主账号管理子账号 | ✅ 完成 | 列出 / 添加 / 停用 / 启用 / 删除子账号（仅 owner 可调用） |
| 3 | 多人命理档案 | ✅ 完成 | 任意账号可录入多个不同人的出生信息并存档、编辑、删除 |
| 4 | 主账号跨账号视角 | ✅ 完成 | 档案列表可切换「全部（含子账号）/ 仅我的」 |
| 5 | 模块一 · 生辰格局 | ✅ 完成 | 四柱 + 纳音 + 十神逐柱 + 主导格局 + 格局层次 + 身强身弱（得令/得地/得势） |
| 6 | 模块二 · 五行分布 | ✅ 完成 | 五行生克图（canvas）+ 旺衰占比条形 + 旺衰解读 |
| 7 | 模块三 · 喜用神 | ✅ 完成 | 用神 / 喜神 / 忌神 + 幸运色 / 数字 / 石 + 解读 |
| 8 | 真太阳时 + 夏令时校正 | ✅ 完成 | 按出生城市经度校正；1986–1991 大陆夏令时扣回 |
| 9 | 新中式视觉 | ✅ 完成 | 宣纸米白/朱砂/黛青配色、留白、印章序号（壹贰叁）、**再生纸粗糙纹理背景**（SVG feTurbulence 噪声，无叶纹） |
| 10 | 免责声明 | ✅ 完成 | 结果页附「娱乐参考 · 不构成专业建议」 |
| 11 | 越权隔离 | ✅ 完成 | 子账号无法读写他人（含其他子账号）档案；接口层统一用 getAllowedOwners 校验 |
| 12 | 端到端自测 | ✅ 完成 | test-e2e.js 覆盖注册/登录/录入/分析/跨账号/越权/停用，全过 |
| 13 | 部署上线（GitHub + Render） | ✅ 完成 | 已 push 到 `github.com/chengxiaomiao/xingzuo-mingli`，并在 Render 免费层上线：**https://xingzuo-mingli.onrender.com**（根路径 `/` 已重定向到 `/login.html`） |

> ⚠️ **已移除模块**：原「适合行业」模块（模块四）已在重构时全链路删除（UI/状态/引擎/路由/预览均无残留）—— 不要再把它加回来，除非用户明确要求。
> ⚠️ **视觉约定变更**：早期曾用「真实植物叶纹暗纹」铺背景，但**当前版已彻底移除叶纹**，改用程序化「再生纸粗糙纹理」。`style.css` 中**不再有 `--leaf` 变量**，相关纹理现在叫 `--paper-mottle` / `--paper-grain`。后续视觉改动不要再回加 `--leaf`。

---

## 4. 关键代码逻辑摘要

### 4.1 鉴权与多账号模型
- **注册/登录**（`auth.js`）：密码用 `sha256(salt + password)` 存储；登录成功下发随机 `token`（当前**不校验过期时间**，仅校验是否存在/停用）；首个注册者 `role=owner`，其余默认走主账号添加。
- **鉴权中间件**（`middleware/auth.js`）：从 `Authorization: Bearer <token>` 取 token，查 `users` 表；无记录或 `status=disabled` 返回 401/403。
- **跨账号权限**（`db.js` 的 `getAllowedOwners(username)`）：
  - 子账号 → 仅自己一个 owner。
  - 主账号 → 自己 + 所有 `owner=自己` 的子账号。
  - 所有档案/分析类接口都先用它过滤 `WHERE owner IN (...)`，实现越权隔离。

### 4.2 命理引擎 `bazi.js → analyze(birthInfo)`（返回约 30 个字段）
输入 `birthInfo`：`{ calendarType('solar'|'lunar'), year, month, day, timeMode('precise'|'range'), hour, minute, range(时辰名), gender, city }`
核心步骤：
1. **真太阳时校正** `toTrueSolar()`：农历先转公历；按城市经度 `(lng-120)*4` 分钟校正；1986–1991 夏令时扣 60 分钟；输出 `trueSolar` 字符串。
2. **四柱**：`lunar-javascript` 的 `Solar.getLunar().getEightChar()` 取年/月/日/时柱。
3. **五行统计**：天干 + 地支本气各计 1。
4. **身强身弱**：以日主强弱 `support>=drain` 判定偏强/偏弱；并拆解「得令（月令同我/生我）、得地（地支藏干有日主本气）、得势（天干比劫印星帮身）」三端。
5. **十神与格局**：以日干为基准算年干/月干/时干十神 + 月支本气十神（取格）；`buildPatternName` 把月支本气十神映射为格局名（如七杀格、正官格、建禄格等）；`buildPatternLevel` 按「月干与月支本气是否同气」粗略判定格局层次。
6. **喜用神推导**（简化扶抑法）：偏强 → 喜「克泄耗」(我生/我克/克我)；偏弱 → 喜「生扶」(日主/生我)。取候选中出现最少的五行作主用神 `primaryElement`；再推喜神(生用神者)、忌神(克耗用神者，且剔除与喜神冲突者)。
7. **输出关键字段**：`pillars, zodiac, dayMaster, dayMasterElement, wuxing, strong, strongText, strongDetail, shiShen, shiShenDetail, pattern, patternLevel, patternText, nayin, wuxingBars, wuxingPercent, wuxingAnalysis, xiyong, primaryElement, xiShen, jiShen, xiyongDetail, luckyColor, luckyNumber, luckyStone, luckyDetail, trueSolar`。

### 4.3 接口契约（前端必须按此对接）
- `GET/POST/DELETE /api/profiles` 与 `/api/profiles/:id`：档案 CRUD。POST 请求体为 `{ personName, birthInfo, recordId? }`；**birthInfo 是嵌套对象，不是摊平字段**（这是早期联调踩过的坑）。
- `POST /api/analyze`：请求体 `{ recordId }`；响应为 `{ success, result, personName }`，前端读 `data.result`（注意不是 `data.data`）。
- `POST /api/admin`：请求体 `{ action:'add'|'disable'|'enable'|'remove', username?, password?, target? }`。**正确端点是 `/api/admin`，不是 `/api/admin/members`**（历史命名坑）。
- `POST /api/auth/register|login|logout`。
- 所有受保护接口需带 `Authorization: Bearer <token>`。

### 4.4 前端渲染（`result.js`）
三模块用印章序号「壹/贰/叁」分块拼 HTML；五行生克图 `drawWuxing()` 用 canvas 画五要素圆环，实线连「生」、虚线连「克」，节点圆内显示该元素与占比。所有动态文本经 `escapeHtml()` 转义防 XSS。

### 4.5 视觉实现（`web/css/style.css`）
- **背景**：`body` 底色 `--paper`(#efe7d4) 之上叠加两层 SVG `feTurbulence` 噪声纹理：
  - `--paper-mottle`（base64 SVG，大尺度斑驳，baseFrequency 0.014 / 5 阶）→ 再生纸云絮纤维块。
  - `--paper-grain`（base64 SVG，细颗粒，baseFrequency 0.85）→ 纸面磨砂质感。
  - 通过 `background-image: var(--paper-grain), var(--paper-mottle)` 叠加，无任何叶纹。
- **卡片白底**：`--card: rgba(252, 248, 240, 0.66)`（透明度 0.66，比早期 0.92 更通透，背后的纸纹可透出，不抢焦点）。
- **配色**：`--ink:#2e2a25` / `--ink-soft:#6a6358` / `--cinnabar:#9e3d34` / `--dai:#3f5a6b` / `--gold:#a8854e` / `--line:rgba(46,42,37,0.12)`。
- **注意**：早期曾用 `--leaf` 叶纹变量，已彻底删除；现在纹理变量是 `--paper-mottle` / `--paper-grain`，不要再用 `--leaf` 或 `__LEAF__` 占位。

---

## 5. 已知的待修复问题或未完成功能

**部署相关（已全部完成 ✅）**
- [x] 推送到 GitHub 仓库（`github.com/chengxiaomiao/xingzuo-mingli`，本地 git 身份 `chengxiaomiao` / `847075892@qq.com`），最新提交 `ebf5652`（feat: migrate to LibSQL + Turso for persistent data）。
- [x] Render 免费层上线：`https://xingzuo-mingli.onrender.com`。
  - 踩坑：Render 原自动选 Node 26 导致 `better-sqlite3` 编译失败 → 已锁 `engines.node=22.x`，重部署即过。
  - 上线后根路径 `Cannot GET /` → 已在 `server/index.js` 加 `app.get('/', redirect '/login.html')` 修复。
- [x] **配置 Turso 持久化**：在 Render Environment 填 `LIBSQL_URL`+`LIBSQL_AUTH_TOKEN` 指向 Turso 数据库，数据写入云端，**彻底根治早期「换设备登录数据丢失」问题**（跨设备 / 重部署均不丢）。

**功能缺口（可按需排期）**
- [ ] **忘记密码 / 改密码**：当前无此功能，密码仅注册/主账号添加时设定。
- [ ] **公共注册策略**：首个账号后公共注册即关闭，属设计如此；如需开放注册需另加开关。
- [ ] **分页**：档案列表 `LIMIT 200`，超量无分页。
- [ ] **Token 过期未校验**：`tokenExpire` 已存但未在服务端判断，token 长期有效直至登出/停用。

**已知注意点 / 轻微问题**
- [ ] `test-e2e.js` 第 2、15 行注释仍写「内存 MongoDB」，系早期 MongoDB 时期的**过时注释**，实际代码已用 LibSQL（本地 `file:` 模式）；运行 `node test-e2e.js` 会在同进程内启动服务并自测，需确保 3000 端口空闲。
- [ ] 城市经度表 `CITY_LONGITUDE` 仅覆盖约 45 个主要城市；未知城市默认按北京经度（116.4）校正，偏远城市真太阳时会有偏差。
- [ ] 喜用神为**简化扶抑法**，格局层次为粗略判定，文档与页面已注明「仅供参考 / 娱乐」。
- [x] **数据持久化（本次迁移目标，已完成 ✅）**：数据库已从「本地 SQLite 临时盘」迁到「LibSQL + Turso 托管」。已于 Render 控制台 Environment 填好 `LIBSQL_URL` 与 `LIBSQL_AUTH_TOKEN`，数据现写入 Turso 云端，**换设备登录 / 重部署都不会丢**。详见第 7 节第 5 点（Turso 一步到位）。
- [ ] 无 HTTPS 强制/无速率限制（Render 已提供 HTTPS 入口，基本够用）。
- [ ] 视觉为程序化再生纸纹理（非真实图片），若想更明显/更淡，调 `--paper-mottle`/`--paper-grain` 两层 SVG 的 `feComponentTransfer` 的 `slope`（透明度）或 `feTurbulence` 的 `baseFrequency`（颗粒密度）即可。

---

## 6. 当前使用的依赖库及版本号

**运行时依赖（`package.json` → dependencies）**：
| 包 | 版本 | 用途 |
|----|------|------|
| express | ^4.19.2 | Web 框架 / 路由 / 静态托管 |
| @libsql/client | ^0.17.4 | LibSQL 异步客户端（纯 JS，无需原生编译）；远程 Turso 或本地 file: 自动切换 |
| cors | ^2.8.5 | 跨域（本地同源其实可不依赖，保留无害） |
| lunar-javascript | ^1.6.18 | 四柱/农历/真太阳时计算引擎 |

**其它环境**：
- Node.js：**22.x**（已在 `engines` 锁定；现用纯 JS 的 `@libsql/client`，不再有原生编译问题）。
- 无构建工具、无前端框架、无 TypeScript。

**scripts**：`start` = `node server/index.js`，`dev` = `node server/index.js`（两者相同）。

---

## 7. 快速恢复上下文 / 上手步骤（给接手对话用）

1. **本地跑起来**：
   ```bash
   cd xingzuo-mingli-web
   npm install        # 纯 JS 依赖，无原生编译
   npm start          # 监听 http://localhost:3000（默认 file: 本地数据库）
   ```
   浏览器开 `http://localhost:3000/login.html` → 先去「注册」建第一个账号（自动成主账号）。

2. **跑自测**：`node test-e2e.js`（需 3000 端口空闲），应全部 ✓。

3. **线上访问**：已部署到 Render 免费层，线上地址 **https://xingzuo-mingli.onrender.com**（根路径 `/` 自动跳转 `/login.html`）。代码在 `github.com/chengxiaomiao/xingzuo-mingli`，Render 连仓库自动读 `render.yaml` 部署；Node 版本锁定 22.x。

4. **关键约定（避免重踩坑）**：
   - 出生信息前端发**嵌套 `birthInfo` 对象**；分析响应读 `data.result`；子账号管理走 `/api/admin` 带 `action` 字段。
   - 视觉纹理在 `web/css/style.css` 的 `--paper-mottle` / `--paper-grain`（base64 SVG feTurbulence），**没有 `--leaf`**，也不要去找 `__LEAF__` 占位（早已内联/移除）。
   - 数据库默认 `data/mingli.db`（本地 file:），已被 git 忽略；**配置 `LIBSQL_URL`+`LIBSQL_AUTH_TOKEN` 即切到 Turso 托管**，代码无需改。
   - 用户（chengxiaomiao）倾向**自己在本机终端执行 git push**，不把 GitHub PAT/Token 明文发给 AI；如需推送请给用户完整命令、由用户自行运行。

5. **让数据持久（多设备共享 / 重部署不丢）— Turso 一步到位**：
   - 注册 [turso.tech](https://turso.tech) → 建一个数据库（如 `mingli`）→ 复制「Database URL」(形如 `libsql://xxxx.turso.io`) 和「Auth Token」。
   - 在 Render 服务页 → **Environment** → 添加两个变量：
     - `LIBSQL_URL` = 上面的 Database URL
     - `LIBSQL_AUTH_TOKEN` = 上面的 Auth Token
   - 保存后 **Redeploy**。之后数据存在 Turso（云端），换设备登录 / 重部署都还在。
   - 凭据由用户自行填写，**不要写进代码或仓库**。

---

## 8. 视觉迭代记录（2026-08-13，备查）

**① 第一次（调淡叶纹 + 对齐小程序）**
- 问题：网页版背景叶纹过深、文字对比度不足。
- 根因：网页版 `--leaf` 叶纹 SVG 的 `<g>` 分组无 `opacity`（描边满透明度），而小程序同款叶纹带 `opacity≈0.10`。
- 修复：`--leaf` 32 个 `<g>` 加 `opacity="0.10"`；`.card::before` 透明度 0.5→1；`--ink-soft` #5a5249→#6a6358；`1rpx`→`1px`（CSS 8 处 + entry.html 1 处，修复浏览器不识别小程序单位导致边框消失）。

**② 第二次（彻底移除叶纹 + 再生纸纹理）— 当前最终状态**
- 问题：用户要求移除叶纹、纯色背景换成「再生纸粗糙纹理质感」、白底更通透。
- 修复（`web/css/style.css`）：
  - 删除 `--leaf` 变量定义、`body` 的 `background-image: var(--leaf)`、`.card::before` 叶纹伪元素（grep leaf 已无残留）。
  - 新增 `--paper-mottle`（大尺度斑驳）+ `--paper-grain`（细颗粒）两层 SVG `feTurbulence` 噪声，body 以 `background-image: var(--paper-grain), var(--paper-mottle)` 叠加在 `--paper` 底色上。
  - `--card` 透明度 `rgba(252,248,240,0.92)` → `0.66`（白底更通透，露出纸纹）。
- 验证：两层纹理 SVG 解码合法（含 feTurbulence）；CSS 与 login.html 均返回 200；`.card::before` 已移除。

*—— 文档结束。新对话只需把本文件内容贴给我，并告诉我下一步要做的事（如「加忘记密码功能」/「改视觉纹理密度」）即可。*
